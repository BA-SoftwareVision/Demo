name: Deploy (Admin Approved - No Docker)

on:
  push:
    branches:
      - main # runs after merge to main

jobs:
  deploy:
    runs-on: ubuntu-latest

    # 🔒 ADMIN APPROVAL GATE
    environment:
      name: production

    steps:
      # Checkout code AFTER approval
      - name: Checkout code
        uses: actions/checkout@v4

      # Simulated deployment step
      - name: Deploy application
        run: |
          echo "✅ Admin approved deployment"
          echo "🚀 Application would be deployed now"
